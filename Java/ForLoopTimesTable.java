
public class ForLoopTimesTable
{
	public static void main(String args[])
	{
		int timestable = 8;
		int loop;
		for(loop = 1; loop <= 12; loop++)
			System.out.println(loop + " * " + timestable + " = " + loop * timestable);
	}
}
